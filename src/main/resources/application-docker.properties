# Application Name
spring.application.name=controle-gastos

# Server Configuration
server.port=8080
server.servlet.context-path=/

# MongoDB Atlas Configuration (Docker profile)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://minnaelcampelo:1324@gastos.tqfe3ln.mongodb.net/controle_gastos?retryWrites=true&w=majority}
spring.data.mongodb.database=controle_gastos

# Logging Configuration
logging.level.root=INFO
logging.level.com.minnael.controle_gastos=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.boot.autoconfigure=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Actuator Configuration
management.endpoint.health.show-details=always
management.health.mongo.enabled=false

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true

# JWT Configuration (mesma secret do microserviço Node.js)
jwt.secret=${JWT_SECRET:sua-chave-secreta-aqui-minimo-256-bits}
jwt.expiration=3600000
jwt.header=Authorization
jwt.prefix=Bearer

# Actuator (para health checks no Docker)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.defaults.enabled=true
