# Environment variables for Node.js Auth Service
NODE_ENV=production
PORT=3000

# JWT Secret (deve ser a mesma da API Spring Boot)
JWT_SECRET=your-super-secret-jwt-key-at-least-256-bits

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/auth?retryWrites=true&w=majority
