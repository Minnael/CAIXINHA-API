# ========================================
# VARIÁVEIS DE AMBIENTE - DOCKER
# ========================================
# COPIE ESTE ARQUIVO PARA .env E CONFIGURE OS VALORES

# JWT Secret (MESMA chave para auth-service e api-service)
# IMPORTANTE: Gere uma chave aleatória de pelo menos 256 bits
# Comando para gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-at-least-256-bits-please-change-this

# MongoDB Atlas - Auth Service
# URI de conexão do MongoDB para o serviço de autenticação
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/auth?retryWrites=true&w=majority

# MongoDB Atlas - API Service (Controle de Gastos)
# URI de conexão do MongoDB para a API de gastos
SPRING_DATA_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/controle_gastos?retryWrites=true&w=majority

# Portas dos serviços
AUTH_SERVICE_PORT=3000
API_SERVICE_PORT=8080

# ========================================
# NOTAS DE SEGURANÇA
# ========================================
# 1. NUNCA commite este arquivo .env no Git
# 2. Em produção, use secrets management (AWS Secrets Manager, Azure Key Vault, etc.)
# 3. Mantenha JWT_SECRET seguro e rotacione periodicamente
# 4. Use credenciais diferentes para dev/staging/produção
